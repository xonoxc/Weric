services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: weric
      POSTGRES_USER: weric
      POSTGRES_PASSWORD: weric
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
